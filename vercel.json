{
  "buildCommand": "cd ai-cricket-predictor && npm install && npm run build",
  "outputDirectory": "ai-cricket-predictor/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
